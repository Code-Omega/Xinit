server {
    listen 80;
    server_name localhost;

    location = /favicon.ico { access_log off; log_not_found off; }

    location / { try_files $uri @app; }
    location @app {
        include     uwsgi_params;
        uwsgi_pass  unix:/home/pi/Documents/Xinit/Xinit.sock;
    }

}

